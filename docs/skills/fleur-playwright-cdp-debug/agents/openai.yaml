interface:
  display_name: "Playwright CDP Frontend Debug"
  short_description: "Debug fleur frontends through CDP"
  default_prompt: "Use $fleur-playwright-cdp-debug to inspect and debug a fleur frontend through the CDP browser."
