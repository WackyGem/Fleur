# Raw 数据画像：<dataset>

日期：YYYY-MM-DD

状态：Draft

关联：

- 数据契约：`pipeline/contracts/datasets/<dataset>.yml`
- dbt source：`source('raw', '<dataset>')`
- 生成的 source catalog：`pipeline/elt/models/sources.yml`
- 计划中的 staging model：`pipeline/elt/models/staging/<source>/stg_<source>__<entity>.sql`

## 1. 范围与执行信息

- source 名称：
- raw 表：
- profiling 命令：
- 行数：
- 数据范围：
- 分区范围：
- 契约数据集：
- ClickHouse raw 表：
- 表说明：

## 2. 数据分析发现

基于当前 raw 表的现状分析：

- 数据量与覆盖
  - 总记录数：
  - 覆盖主体数：
  - 日期 / 分区范围：
- 粒度与候选键
  - 观察到的粒度：
  - 候选自然键去重结果：
  - 旧候选键或备选键对比：
- 缺失与占位
  - 关键字段 NULL / 空字符串分布：
  - 占位值：
  - 预期缺失：
- 格式与参照完整性
  - 证券代码 / 报告期 / 高价值字符串格式：
  - 直接 raw input 参照命中情况：
- 分布与相关性
  - 枚举 top values：
  - 少量值 / 长尾文本：
  - 字段间强相关：
- 时间字段合理性
  - 日期范围：
  - 日期先后关系异常：
  - 批次时间范围：
- 数值字段合理性
  - 负数 / 零值 / 极端值：
  - 单位判断：
- 其他观察
  - 对 staging 设计有影响、但不应在 staging 静默修正的事实：

## 3. 粒度与键

- 观察到的粒度：
- 候选自然键：
- 重复检查：
- 粒度注意事项：

## 4. 字段画像

| 字段 | 类型 | NULL 数 | 空值/占位值 | 去重/样例 | 备注 |
|------|------|---------|-------------|-----------|------|

## 5. 关键字段发现

### 证券代码字段

- 观察到的格式：
- 无效样例：
- 建议 staging 处理：

### 日期与时间字段

- 范围：
- 无效值或占位值：
- 建议 staging 处理：

### 枚举字段

- 取值：
- 未知或异常取值：
- 建议 staging 处理：

### 数值字段

- 最小/最大值：
- 负数/零值/极端值：
- 单位假设：
- 建议 staging 处理：

## 6. 数据质量问题

| 问题 | 严重程度 | 证据 | staging 处理 | 延后处理 |
|------|----------|------|--------------|----------|

## 7. Staging 设计决策

- 重命名：
- 类型转换：
- 标准化：
- NULL 处理：
- 测试：
- YAML 元数据：

## 8. 延后到 Intermediate/Mart

- 跨源 join：
- 需要优先级判断的去重：
- 主数据修正：
- 粒度变化：
- 业务指标逻辑：

## 待确认问题

- [ ] 问题：

## 关键 SQL 证据摘要

- 行数：
- 日期 / 分区范围：
- 候选键重复：
- 关键 NULL / 占位值：
- 枚举 / 文本分布：
- 数值范围：

## 9. 验收清单

- [ ] 已抽样 raw source。
- [ ] 已记录行数和日期/分区范围。
- [ ] 已评估粒度和候选键。
- [ ] 已完成关键字段画像。
- [ ] 已列出 staging 转换建议。
- [ ] 已列出延后处理事项。
- [ ] 已提出测试或明确豁免。

## Profiling SQL 与结果

完整 SQL 和输出粘贴在此处，作为上方结论的证据附录。
