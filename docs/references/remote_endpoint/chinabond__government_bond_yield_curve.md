# ChinaBond 国债收益率曲线历史查询

本文记录 `chinabond__government_bond_yield_curve` 的远端接口调研结果。调研时间：2026-06-16。

## Endpoint

```text
GET https://yield.chinabond.com.cn/cbweb-czb-web/czb/historyQuery
```

公开 GET 接口。当前探测不需要认证、Cookie 或 Referer；建议保留浏览器 `User-Agent` 和 JSON `Accept` header，否则服务端可能返回 `text/plain;charset=ISO-8859-1`，中文曲线名称会退化为问号。

## 完整请求示例

```text
https://yield.chinabond.com.cn/cbweb-czb-web/czb/historyQuery?startDate=2025-06-17&endDate=2026-06-16&gjqx=0&locale=cn_ZH&qxmc=1
```

建议 headers：

```text
User-Agent: Mozilla/5.0
Accept: application/json,text/plain,*/*
```

## Query Parameters

| 参数 | 必填 | 示例值 | 说明 |
|------|------|--------|------|
| `startDate` | 是 | `2025-06-17` | 查询开始日期，格式 `YYYY-MM-DD` |
| `endDate` | 是 | `2026-06-16` | 查询结束日期，格式 `YYYY-MM-DD` |
| `gjqx` | 是 | `0` | 期限选择。`0` 返回全期限；其他值返回单一期限列，未完整枚举 |
| `locale` | 是 | `cn_ZH` | 语言。`en_US` 会把 `qxmc` 返回为英文 |
| `qxmc` | 是 | `1` | 曲线名称选择。`1` 为中债国债收益率曲线；`2` 为中国地方政府债券收益率曲线 |

本数据集建议固定使用 `gjqx=0&locale=cn_ZH&qxmc=1`。

## 响应结构

成功响应：

```json
{
  "heList": [
    {
      "workTime": "2026-06-16",
      "threeMonth": "1.09",
      "sixMonth": "1.13",
      "oneYear": "1.20",
      "twoYear": "1.28",
      "threeYear": "1.31",
      "fiveYear": "1.46",
      "sevenYear": "1.58",
      "tenYear": "1.73",
      "fifteenYear": null,
      "twentyYear": null,
      "thirtyYear": "2.22",
      "qxmc": "中债国债收益率曲线"
    }
  ],
  "flag": "0"
}
```

无数据或参数错误时通常仍返回 HTTP 200，但业务字段为：

```json
{
  "heList": null,
  "flag": "1"
}
```

## 字段

| 字段 | 样例 | 说明 |
|------|------|------|
| `workTime` | `2026-06-16` | 数据日期。响应按日期倒序返回 |
| `threeMonth` | `1.09` | 3 个月收益率，百分比字符串 |
| `sixMonth` | `1.13` | 6 个月收益率，百分比字符串 |
| `oneYear` | `1.20` | 1 年收益率，百分比字符串 |
| `twoYear` | `1.28` | 2 年收益率，百分比字符串 |
| `threeYear` | `1.31` | 3 年收益率，百分比字符串 |
| `fiveYear` | `1.46` | 5 年收益率，百分比字符串 |
| `sevenYear` | `1.58` | 7 年收益率，百分比字符串 |
| `tenYear` | `1.73` | 10 年收益率，百分比字符串 |
| `fifteenYear` | `null` | 15 年收益率；国债曲线样本中为 `null` |
| `twentyYear` | `null` | 20 年收益率；国债曲线样本中为 `null` |
| `thirtyYear` | `2.22` | 30 年收益率，百分比字符串 |
| `qxmc` | `中债国债收益率曲线` | 曲线名称 |

注意：

- 收益率字段在 JSON 中是字符串或 `null`，不是数字。
- 当 `gjqx` 不是 `0` 时，未选中的期限可能返回空字符串或空格；固定 `gjqx=0` 可以避免大部分空字符串。
- `fifteenYear` 和 `twentyYear` 对国债曲线当前样本为 `null`，但 contract 仍应保留 nullable 数值字段，避免未来上游补齐期限时破坏 schema。

## 探测发现

### 数据范围

接口可查到的最早数据为 `2006-03-01`。查询 `2006-02-01` 到 `2006-03-10` 时，只返回 `2006-03-01` 之后的数据；查询完全早于起始日的区间返回 `flag=1, heList=null`。

### 时间窗口

业务口径说明单次请求范围不超过一年。实际探测中，`2025-01-01` 到 `2026-06-16` 返回 HTTP 200 且 `flag=0`，但生产采集仍应遵守一年以内窗口，按自然年切分请求。

### 空区间

- 起止日期倒置，例如 `startDate=2026-06-16&endDate=2026-06-01`，返回 `flag=1, heList=null`。
- 完全无数据的周末区间，例如 `2026-06-13` 到 `2026-06-14`，返回 `flag=1, heList=null`。
- 未来区间，例如 `2030-01-01` 到 `2030-01-10`，返回 `flag=1, heList=null`。

### 年度样本计数

按自然年请求 `qxmc=1&gjqx=0` 的探测结果：

| year | flag | row_count | first_work_time | last_work_time |
|------|------|-----------|-----------------|----------------|
| 2006 | 0 | 214 | 2006-12-31 | 2006-03-01 |
| 2007 | 0 | 249 | 2007-12-29 | 2007-01-04 |
| 2008 | 0 | 251 | 2008-12-31 | 2008-01-02 |
| 2009 | 0 | 250 | 2009-12-31 | 2009-01-04 |
| 2010 | 0 | 250 | 2010-12-31 | 2010-01-04 |
| 2011 | 0 | 250 | 2011-12-31 | 2011-01-04 |
| 2012 | 0 | 249 | 2012-12-31 | 2012-01-04 |
| 2013 | 0 | 250 | 2013-12-31 | 2013-01-04 |
| 2014 | 0 | 250 | 2014-12-31 | 2014-01-02 |
| 2015 | 0 | 249 | 2015-12-31 | 2015-01-04 |
| 2016 | 0 | 251 | 2016-12-31 | 2016-01-04 |
| 2017 | 0 | 251 | 2017-12-31 | 2017-01-03 |
| 2018 | 0 | 252 | 2018-12-31 | 2018-01-02 |
| 2019 | 0 | 250 | 2019-12-31 | 2019-01-02 |
| 2020 | 0 | 249 | 2020-12-31 | 2020-01-02 |
| 2021 | 0 | 250 | 2021-12-31 | 2021-01-04 |
| 2022 | 0 | 250 | 2022-12-31 | 2022-01-04 |
| 2023 | 0 | 250 | 2023-12-31 | 2023-01-03 |
| 2024 | 0 | 251 | 2024-12-31 | 2024-01-02 |
| 2025 | 0 | 248 | 2025-12-31 | 2025-01-02 |
| 2026 | 0 | 111 | 2026-06-16 | 2026-01-04 |

截至 2026-06-16，全历史按年切片合计 5,075 行。

## Parquet 可行性

结论：适合作为年分区 source Parquet。

推荐设计：

- `source`: `chinabond`
- `dataset`: `chinabond__government_bond_yield_curve`
- `source_asset_key`: `["source", "chinabond__government_bond_yield_curve"]`
- `storage_mode`: `partitioned`
- `partition_key_name`: `year`
- 历史分区范围：`2006` 到当前年
- 首年请求窗口：`2006-03-01` 到 `2006-12-31`
- 当前年请求窗口：`YYYY-01-01` 到运行日
- 其他年份请求窗口：`YYYY-01-01` 到 `YYYY-12-31`
- 主键建议：`work_date` 或 `year + work_date`。若未来支持多曲线同表，再扩展为 `curve_code + work_date`

推荐 Parquet 字段：

| 字段 | 建议类型 | 来源 | 说明 |
|------|----------|------|------|
| `year` | int32 | `workTime` | 分区年 |
| `work_date` | date32 | `workTime` | 数据日期 |
| `curve_name` | string | `qxmc` | 曲线名称 |
| `three_month_yield_pct` | decimal 或 float64 | `threeMonth` | 百分比数值 |
| `six_month_yield_pct` | decimal 或 float64 | `sixMonth` | 百分比数值 |
| `one_year_yield_pct` | decimal 或 float64 | `oneYear` | 百分比数值 |
| `two_year_yield_pct` | decimal 或 float64 | `twoYear` | 百分比数值 |
| `three_year_yield_pct` | decimal 或 float64 | `threeYear` | 百分比数值 |
| `five_year_yield_pct` | decimal 或 float64 | `fiveYear` | 百分比数值 |
| `seven_year_yield_pct` | decimal 或 float64 | `sevenYear` | 百分比数值 |
| `ten_year_yield_pct` | decimal 或 float64 | `tenYear` | 百分比数值 |
| `fifteen_year_yield_pct` | decimal 或 float64 nullable | `fifteenYear` | 15 年收益率 |
| `twenty_year_yield_pct` | decimal 或 float64 nullable | `twentyYear` | 20 年收益率 |
| `thirty_year_yield_pct` | decimal 或 float64 | `thirtyYear` | 30 年收益率 |

落地时应显式把空字符串、空格和 `null` 统一转换为 null，再转换为数值类型。字段名建议保留 `_pct` 后缀，明确单位是百分比点而不是比例值。

## ClickHouse raw 同步建议

如果后续启用 raw sync，适合使用当前 contract 支持的 `partition_strategy: year`。

需要注意：

- 不应依赖 A 股交易日历驱动该数据；接口返回的 `workTime` 才是事实日期。
- 由于接口历史数据规模很小，年度分区回填成本低，可以直接按年回填。
- 当前年分区可每日刷新。历史年份通常不需要定期刷新，除非发现上游会修正历史。
- 业务错误 `flag=1` 不能一律视为失败：未来年、纯空日期区间、完全早于 2006-03-01 的窗口都是空结果；但目标年份内预期有数据却返回空，应记录异常 metadata。

## 待核实

- 官方参数枚举：`gjqx` 的完整期限映射只做了部分探测，生产数据集建议先固定 `gjqx=0`。
- 官方发布日历：`workTime` 不完全等同 A 股交易日，应避免用现有 A 股交易日历做完整性断言。
- 历史修正策略：尚未确认上游是否会回改历史年份；初始实现可先支持按年手动重刷。
