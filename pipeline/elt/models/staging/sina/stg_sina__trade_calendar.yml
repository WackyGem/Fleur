version: 2

models:
  - name: stg_sina__trade_calendar
    description: 新浪 A 股交易日历 staging 模型。
    columns:
      - name: trade_date
        description: "{{ doc('field_trade_date') }}"
        data_type: Date
        data_tests:
          - not_null
          - unique
        config:
          meta:
            glossary_key: trade_date
            source_columns:
              - source: raw
                table: sina__trade_calendar
                column: trade_date
