# Racingline Styles

Global Tailwind and shadcn/ui tokens live in `../index.css`, as configured by `components.json`.

Keep additional style files out of this directory unless a future ADR or system map update defines a reusable style boundary.

